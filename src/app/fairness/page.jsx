import Link from 'next/link';

export const metadata = { title: 'Fairness', description: 'How AptCasino verifies Inco game results.' };

export default function FairnessPage() {
  return <main className="min-h-screen bg-[#080812] px-5 py-16 text-white"><div className="mx-auto max-w-4xl"><p className="text-xs font-black uppercase tracking-[.28em] text-emerald-300">Verifiable confidentiality</p><h1 className="mt-4 font-display text-5xl font-black sm:text-7xl">The browser never chooses the winner.</h1><p className="mt-6 text-lg leading-8 text-white/60">Each round stores an encrypted Inco seed handle. Settlement succeeds only when the submitted attestation matches that exact handle and the Inco verifier accepts the covalidator signatures.</p><div className="mt-10 grid gap-4 sm:grid-cols-3">{[['1','Sealed draw','e.rand() creates the hidden seed.'],['2','Bound proof','The attestation handle must equal the round handle.'],['3','On-chain payout','Game rules derive the result after verification.']].map(([n,t,d]) => <div key={n} className="rounded-2xl border border-white/10 bg-white/[.03] p-6"><span className="text-emerald-300">0{n}</span><h2 className="mt-3 font-black">{t}</h2><p className="mt-2 text-sm leading-6 text-white/50">{d}</p></div>)}</div><Link href="/game" className="mt-9 inline-block rounded-xl bg-white px-6 py-3 font-black text-black">Play a confidential round</Link></div></main>;
}
