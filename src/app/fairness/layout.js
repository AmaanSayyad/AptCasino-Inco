export const metadata = {
  title: 'Fairness',
  description: 'How AptCasino verifies Inco Lightning game results on Base Sepolia.',
};

export default function FairnessLayout({ children }) {
  return children;
}
